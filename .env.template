# Sandbox Provider - Choose "e2b", "daytona", or "docker"
# If not set, will auto-detect: E2B (if key) > Daytona (if key) > Docker (fallback)
SANDBOX_PROVIDER=
NEXT_PUBLIC_SANDBOX_PROVIDER=

# E2B Configuration - Get your API key here: https://e2b.dev/
E2B_API_KEY=

# Daytona Configuration - Get your API key here: https://daytona.io/
DAYTONA_API_KEY=
DAYTONA_API_URL=https://app.daytona.io/api
DAYTONA_TARGET=us

# Docker Configuration (auto-selected if no E2B or Daytona keys)
# For local Docker: leave DOCKER_HOST empty, defaults to /var/run/docker.sock
# For remote Docker: set DOCKER_HOST (e.g., tcp://192.168.1.100:2375)
DOCKER_HOST=
DOCKER_SOCKET=/var/run/docker.sock
DOCKER_NETWORK=palmframe-sandbox

# Mistral API Key - https://console.mistral.ai/
MISTRAL_API_KEY=

# PostgreSQL Database
DATABASE_URL=postgresql://palmframe:palmframe_dev_password@localhost:5432/palmframe

# Optional env vars
NEXT_PUBLIC_SITE_URL=
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_WINDOW=60
